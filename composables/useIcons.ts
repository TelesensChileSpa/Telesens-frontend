import { Sun, Moon, Eye, EyeOff, User, Lock } from 'lucide-vue-next';

export function useIcons() {
    return {
        Sun,
        Moon,
        Eye,
        EyeOff,
        User,
        Lock,
    };
}

// Exporta los íconos individualmente si prefieres hacerlo de esa manera también
export { Sun, Moon, Eye, EyeOff, User, Lock };
