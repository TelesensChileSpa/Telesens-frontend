import { Sun, Moon, Eye, EyeOff, User, Lock, LogIn } from 'lucide-vue-next';

export function useIcons() {
    return {
        Sun,
        Moon,
        Eye,
        EyeOff,
        User,
        Lock,
        LogIn
    };
}

export { Sun, Moon, Eye, EyeOff, User, Lock, LogIn };
